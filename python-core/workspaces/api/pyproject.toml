[project]
name = "pep-api"
version = "0.0.1"
description = "..."

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pep_api"]

[tool.hatch.metadata]
allow-direct-references = true
