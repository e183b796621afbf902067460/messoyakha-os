[project]
name = "pep-lakehouse"
version = "0.0.1"
description = "..."

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pep_lakehouse"]

[tool.hatch.metadata]
allow-direct-references = true
