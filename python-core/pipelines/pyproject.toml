[project]
name = "pep-pipelines"
version = "0.0.1"
description = "..."
readme = "README.md"

[project.urls]
smart-lab-phor-dividends = "https://smart-lab.ru/q/phor/dividend/"

[tool.uv]
package = false

[tool.uv.sources]
pep-api = { workspace = true }
pep-lakehouse = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.build.force-include]
"src" = "pep_pipelines"
